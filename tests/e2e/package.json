{
  "name": "opencloudmesh-go-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for OpenCloudMesh Go server",
  "private": true,
  "scripts": {
    "test": "bunx playwright test",
    "test:headed": "bunx playwright test --headed",
    "test:debug": "bunx playwright test --debug",
    "install:browsers": "bunx playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
